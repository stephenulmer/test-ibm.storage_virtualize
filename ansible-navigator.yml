---
ansible-navigator:
  execution-environment:
    image: registry.redhat.io/ansible-automation-platform-25/ee-supported-rhel9 
    pull:
      policy: missing
  logging:
    level: critical
  mode: stdout
  playbook-artifact:
    enable: False
